// sendVerificationEmail.js
// This function will handle sending verification emails to new users.
// TODO: Implement actual email sending logic using nodemailer or Supabase SMTP

async function sendVerificationEmail(email, token) {
  console.log(`Pretend to send email to ${email} with token ${token}`);
}

module.exports = sendVerificationEmail;
